# render.yaml

services:
  - name: web
    buildCommand: |
      pip install --upgrade wheel
      pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT app:app
    env:
      - key: PYTHON_VERSION
        value: python3.9
